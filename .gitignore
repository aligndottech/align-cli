node_modules/
dist/
*.js.map
*.d.ts.map
.env
.env.local

# Build artifacts from `npm pack` (local install/testing)
*.tgz

# This repo uses npm (package-lock.json); never commit a pnpm lockfile
pnpm-lock.yaml

# test coverage
coverage/
