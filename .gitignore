node_modules/
dist/
*.js.map
*.d.ts.map
.env
.env.local

# Build artifacts from `npm pack` (local install/testing)
*.tgz

# This repo uses npm (package-lock.json); never commit a pnpm lockfile
pnpm-lock.yaml

# test coverage
coverage/

# Research, plans and session notes. Kept locally, not published: this repo is public
# and these are working notes, not product documentation. align-stack (private) keeps its
# thoughts/shared/ convention. Untracked in #95; this is the guard that keeps it that way.
thoughts/
